create table order_event (id number generated by default as identity primary key, order_id number not null, event_type varchar2(50) not null, event_time timestamp default systimestamp not null, payload clob, constraint chk_order_event_type check (event_type in ('CREATE', 'UPDATE', 'DELETE')));
